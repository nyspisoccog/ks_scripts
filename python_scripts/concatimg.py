__author__ = 'katie'

import os

src_rt = '/media/truecrypt1/SocCog/Soccog/August2014snapshot/preproc_data/func'

dst_rt = '/media/truecrypt1/SocCog/Soccog/concat_preproc'

subs = []

for sub in subs:
    for dir in os.path.join(src_rt, sub, 'func'):
        for run in os.listdir()
        rcnt = len([run for run in os.listdir(dir) if 'L' in run])

    for



